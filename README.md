# Swift-MVC-Practice-
This is a very basic login swift program following MVC rule.
User data are store in a dictionary inside the model class

In version 1.0.1 I implement the prepareForSegue function to send the flag variable and the username string from the first view controller to the PsychologistViewController